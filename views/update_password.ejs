
<% if(token.isValid){ %>

<form action="/users/new-password/<%= token._id %>" method="POST">
    <input type="password" name="password" required>
    <input type="password" name="confirm_password" required>
    <input type="submit" value="Update Password">
</form>

<% } %>